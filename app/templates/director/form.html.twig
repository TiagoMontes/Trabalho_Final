{% extends 'base.html.twig' %}

{% block title %}
    {{ title }}
{% endblock %}

{% block body %}
    <div class="container text-center">
        <h1>{{ title }}</h1>

        {{ form(director_form) }}

        <a href='{{ path("director_index") }}' class='btn btn-primary mt-3 w-5'>Cancelar</a>
    </div>

{% endblock %}
