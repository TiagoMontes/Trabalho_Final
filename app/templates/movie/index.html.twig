{% extends 'base.html.twig' %}

{% block title %}
    {{ title }}
{% endblock %}

{% block body %}
    <h1 class='mt-3'>{{ title }}</h1>
    <a href="{{ path('movie_add') }}" class='btn btn-primary mt-3'>Adicionar novo filme</a>

    <table class="table mt-3">
        <thead>
            <tr>
                <th scope="col">Filme</th>
                <th scope="col">Descrição</th>
                <th scope="col">Categoria</th>
                <th scope="col">Duração</th>
                {# <th scope="col">Lançamento</th> #}
                <th scope="col">Diretor</th>
                <th scope="col">Ações</th>
            </tr>
        </thead>
        <tbody>
            {% for movie in movies %}
                <tr>
                    <td>{{ movie.title }}</td>
                    <td>{{ movie.description }}</td>
                    <td>{{ movie.category }}</td>
                    <td>{{ movie.duration }} min</td>
                    {# <td>{{ movie.release_date }}</td> #}
                    <td>{{ movie.director }}</td>
                    <td>
                        <a href='{{ path("movie_edit", {id: movie.id}) }}' class='btn btn-primary' name='id'>Editar</a>
                        <a href='{{ path("movie_delete", {id: movie.id}) }}' class='btn btn-danger' name='id'>Excluir</a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <div>
        <a href='{{ path("category_index") }}' class='btn btn-primary mt-3'>Categorias</a>
        <a href='{{ path("director_index") }}' class='btn btn-primary mt-3'>Diretores</a>
    </div>
{% endblock %}
