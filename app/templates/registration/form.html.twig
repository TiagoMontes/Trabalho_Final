{% extends 'base.html.twig' %}

{% block title %}Cadastrar Usuário{% endblock %}

{% block body %}
    <div class="container text-center w-50 p-3 rounded-3 mt-5 card-form">
    <h1>Criar nova conta</h1>
        {{ form_start(registrationForm) }}
            {{ form_row(registrationForm.name) }}
            {{ form_row(registrationForm.email) }}
            {{ form_row(registrationForm.plainPassword, {
                label: 'Password'
            }) }}
            {{ form_row(registrationForm.agreeTerms) }}
            <a href="{{ path('register_terms') }}" class='btn btn-light' target=' _blank'>Termos</a>

        {{ form_end(registrationForm) }}
        <a href='{{ path("login") }}' class='btn btn-primary mt-2 w-100'>Cancelar</a>
    <div>
{% endblock %}
