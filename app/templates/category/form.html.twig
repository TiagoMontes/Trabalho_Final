{% extends 'base.html.twig' %}

{% block title %}
    {{ title }}
{% endblock %}

{% block body %}
    <div class="container text-center w-50 border p-3 rounded-3 mt-5">
        <h1 class="mt-3">{{ title }}</h1>
        {{ form(category_form) }}
        <a href="{{ path('category_index')}}" class=" btn btn-primary mt-3 w-100">Cancelar</a>
    </div>
{% endblock %}
